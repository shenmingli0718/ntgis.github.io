{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python -m pip install -r requirements/production.txt && python manage.py collectstatic --noinput --settings=config.settings.production && python manage.py migrate --settings=config.settings.production && gunicorn --workers=4 config.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
  }