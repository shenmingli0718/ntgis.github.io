{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python -m pip install -r requirements.txt && python int_gis_use_dash.py collectstatic --noinput --settings=config.settings.production && python int_gis_use_dash.py migrate --settings=config.settings.production && gunicorn int_gis_use_dash:server --bind 0.0.0.0:$PORT --workers=4 config.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
  }
